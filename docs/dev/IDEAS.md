# IDEAS — the backlog (just_ai_i18n_docgen)

The one place to drop ideas for this project that aren't yet scheduled work — same
charter as JW's `docs/dev/IDEAS.md`. Not a plan, not a spec — a holding pen so nothing
good gets lost. Committed work lives in `docs/dev/TASKS.md`; when an idea is picked up
it graduates to a plan doc and gets a link here (or is struck through).

- Keep entries short: the itch, the rough shape, why it matters.
- Newest at the top. Date each one.
- Anyone (user or agent) can add here; adding an idea is never "starting" it.

---

*(empty — nothing parked yet)*
